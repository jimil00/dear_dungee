<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Shelter">

    <!--검색어에 따른 보호소 전체 검색(필요물품, 이름, 지역)-->
    <select id="selectShelterListBySw">
        select * from shelter full outer join item
        where item_name like '%' || #{search_word} || '%' or shelter_name like '%' || #{search_word} || '%' or shelter_address like '%' || #{search_word} || '%'
    </select>

</mapper>